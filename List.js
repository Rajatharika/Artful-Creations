import React, {useState} from 'react';
function DeletebleList(){
    const [items, setitems]=useState([
    'Apple',
    'Orange',
    'Mango',
    ]);
    const handleDelete=(index)=>{
    }
}